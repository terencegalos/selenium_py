# Architecture Improvements Backlog

- [ ] Introduce structured logging helper (per-vendor log files)
- [ ] Add retry/backoff wrappers around WebDriver actions
- [ ] Configuration: global defaults + vendor overrides
- [ ] Progress tracking (resume from last URL)
- [ ] Unit tests for `table_gateway`, `active_record`, `domainobject`
- [ ] Optional: output schema versioning and validator
